# Build outputs and machine-specific paths. The APK itself is a build artefact - rebuild
# it with `npm run build:apk` rather than committing binaries.
node_modules/
www/index.html
*.apk
android/local.properties
android/build/
android/app/build/
android/.gradle/
android/capacitor-cordova-android-plugins/
.gradle/
