{
  "name": "raipurone-worker-app",
  "version": "1.0.0",
  "private": true,
  "description": "Android wrapper around the RaipurOne field worker page",
  "scripts": {
    "sync": "node sync-page.js && cap sync android",
    "build:apk": "npm run sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/camera": "^8.2.4",
    "@capacitor/core": "^8.5.1",
    "@capacitor/geolocation": "^8.2.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.1"
  }
}
