// Loaded automatically by react-scripts before every test file.
// Registers the jest-dom matchers (toBeInTheDocument, toHaveAttribute, ...) the
// component tests rely on.
import '@testing-library/jest-dom';
